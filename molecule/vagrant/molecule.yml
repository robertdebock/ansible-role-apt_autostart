---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
lint:
  name: yamllint
driver:
  name: vagrant
  provider:
    name: virtualbox
platforms:
  - name: apt_autostart-debian
    box: debian/stretch64
  - name: apt_autostart-fedora
    box: fedora/30-cloud-base
provisioner:
  name: ansible
  playbooks:
    prepare: ../resources/prepare.yml
    converge: ../resources/playbook.yml
  inventory:
    host_vars:
      apt_autostart-fedora:
        ansible_python_interpreter: /usr/bin/python3
scenario:
  name: vagrant
